export interface User {
  created: string;
  dateOfBirth: string;
  email: string;
  firstName: string;
  lastActive?: string;
  isActive?: boolean
  lastName: string;
  phoneNo: number;
  updated: string;
  userId: string;
}
